import {useState} from 'react';import {useExpenses} from '../hooks/useExpenses';import {money} from '../utils/format';import {ExpenseModal} from '../components/ExpenseModal';export function Expenses(){const [search,setSearch]=useState('');const [page,setPage]=useState(1);const [modal,setModal]=useState(false);const {data,error,loading}=useExpenses(search,page);return <><div className="page-head"><div><h1>Расходы</h1><p>Единый реестр расходов отдела</p></div><button className="primary" onClick={()=>setModal(true)}>+ Добавить расход</button></div><div className="toolbar"><input placeholder="Поиск по партнеру, ИНН, счету…" value={search} onChange={e=>{setSearch(e.target.value);setPage(1)}}/><button>Фильтры</button><button>Настроить колонки</button></div>{loading?<div className="state">Загружаем расходы…</div>:error?<div className="state error">Нет соединения с сервером. {error}</div>:!data?.items.length?<section className="empty"><h2>{search?'Ничего не найдено':'Расходов пока нет'}</h2><p>{search?'Измените запрос или сбросьте фильтры.':'Добавьте первый расход или загрузите счет.'}</p></section>:<div className="table-wrap"><table><thead><tr><th>Период</th><th>Партнер</th><th>Контрагент</th><th>Услуга</th><th>Сумма счетов</th><th>Оплачено</th><th>Остаток</th></tr></thead><tbody>{data.items.map(x=><tr key={x.id}><td>{x.period}</td><td>{x.partner}</td><td>{x.counterparty}</td><td>{x.service_name}</td><td>{money(x.invoice_total)}</td><td>{money(x.paid_total)}</td><td>{money(x.remaining_total)}</td></tr>)}</tbody></table></div>}{modal&&<ExpenseModal close={()=>setModal(false)}/>}</>}
